<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        INTERNET permission is required for the NLE Control API client. Flutter's
        scaffold only declares this in `src/debug/AndroidManifest.xml` (so the
        Flutter tool can communicate with the running app during dev), so it
        merges into debug builds but NOT release builds — release APKs that
        omit this end up unable to open any socket, with no user-visible error
        beyond "Couldn't reach server" at the Dio layer.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--
        usesCleartextTraffic="true": required because the NLE Control API
        is HTTP-only on LAN (per DESIGN §2 + §7.5 — `nest.home`, `192.168.x.x`,
        and similar LAN addresses normalize to http://). Android blocks
        cleartext to non-loopback hosts at targetSdk >= 28 by default;
        without this flag the onboarding "Test connection" silently fails
        with "Couldn't reach server" on every release build.

        Users running their NLE server behind a reverse proxy can still use
        https:// — this flag only permits cleartext, it doesn't require it.
    -->
    <application
        android:label="Rest Thermostat"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
